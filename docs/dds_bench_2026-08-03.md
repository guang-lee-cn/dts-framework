# DDS 链路基准 + 丢包分层（task / data 独立基准，2026-08-03）

> 两个独立基准：**A. task 双向收发**（nfoam ↔ task）、**B. data 单向接收**（spa → data）。
> 每基准 2 张图（部署图 + 流程图，共 4 张，箭头配色区分收发）。测试结果表 + 图 + 丢包定位放一起。

---

## 1. 测试报告

### 1.1 task 双向收发（nfoam 发 32K JSON → task 简析 + 响应 → nfoam 收）

| 发送数 | 速率 | 响应 | 丢包 | 双投 | 往返avg(us) | 往返p99 |
|---|---|---|---|---|---|---|
| 50 | 紧突发 | 50 | 0 | 50 | 847 | 1471 |
| 50 | 1ms | 50 | 0 | 50 | 565 | 924 |
| 50 | 10ms | 50 | 0 | 50 | 1371 | 2239 |
| 50 | 100ms | 50 | 0 | 50 | 2261 | 2880 |
| 100 | 紧突发 | 100 | 0 | 22 | 1186 | 3297 |
| 100 | 1ms | 100 | 0 | 100 | 492 | 722 |
| 100 | 10ms | 100 | 0 | 100 | 1172 | 2005 |
| 100 | 100ms | 100 | 0 | 100 | 2249 | 2792 |
| 200 | 紧突发 | 200 | 0 | 200 | 875 | 2302 |
| 200 | 1ms | 200 | 0 | 200 | 498 | 842 |
| 200 | 10ms | 200 | 0 | 200 | 1322 | 2026 |
| 200 | 100ms | 200 | 0 | 200 | 2231 | 3018 |
| 1000 / 2000 | 紧突发/1ms | 全收 | 0 | - | 430~518 | - |

**>64 变更场景**：零丢（task 消费够快，可靠背压生效）。

### 1.2 data 单向接收（spa 发 ≤32K 结构体 → data 4 字节拷贝，不回响应）

| 发送数 | 速率 | data 收 | 丢包 |
|---|---|---|---|
| 50 | 紧突发 / 1ms / 5ms | 全收 | 0 |
| 200 | 紧突发 / 1ms / 5ms | 全收 | 0 |
| 500 | 紧突发 / 1ms / 5ms | 全收 | 0 |

### 1.3 data 周期 + 突发夹杂（datamix，burst 注入周期流）

| 周期 | burst | 总发 | 收 | 丢 |
|---|---|---|---|---|
| 5ms | 20 | 251 | 251 | 0 |
| 10ms | 20 | 251 | 251 | 0 |
| 5ms | 50 | 551 | 551 | 0 |
| 10ms | 50 | 551 | 551 | 0 |
| 5ms | 100 | 1051 | 1051 | 0 |
| 10ms | 100 | 1051 | 1051 | 0 |

### 1.4 32K 紧突发丢包临界点（task msg1 路径）

| count | task reader 收 | 结论 |
|---|---|---|
| ≤70 | 全收 | **稳定零丢** |
| 80 | 1/81 | **间歇近全丢** |
| 90 | 全收 | 间歇（时好时坏） |
| 100+ | 1-3/101 | **间歇近全丢** |

**触发条件**：紧突发 + 32K + count≥~80 三者叠加；周期 ≥500us 零丢；小包零丢；**非消费者速度**（slow 2000× 仍零丢）；**topic 特定**（data msg3 路径 count=200 仍零丢）。

### 1.5 M 级消息（1MB+）

- **1MB 单条收不到**（SHM 512KB < 1MB，分片也溢出）
- **10M 当前配置不可行**：需 SHM 段放大（自定义 SHM 崩）+ 类型上限 + 分片支持

---

## 2. 基准 A：task 双向收发（nfoam ↔ task）

### A-1. 部署图（线程级 + 组件 + 丢包点，红=发送 / 绿=响应）

```mermaid
flowchart TB
    subgraph NF["进程 nfoam（测试端）"]
        NG["nfoam 主线程<br/>发 32K JSON(msg7) + 订阅响应(msg8)"]
        NW["FastDDS DataWriter(msg7)<br/>Writer 历史（不丢）"]
        NR["FastDDS DataReader(msg8)<br/>收响应 + 量测往返"]
        NG --> NW
        NR --> NG
    end
    subgraph TR["FastDDS 传输层（共享内存）"]
        SHM["SHM 段（512KB ≈ 16 条 32K）<br/>★ 丢包点：段满 + 大突发竞态<br/>实测：快消费者零丢到 2000"]
    end
    subgraph DTS["进程 dts（被测业务）"]
        TR2["FastDDS DataReader(msg7)<br/>task 收 32K JSON"]
        MB["Mailbox(task 桶, 无界)<br/>不丢"]
        T["task 线程<br/>简析 JSON + 响应(msg8)"]
        TR2 --> MB --> T
        TW["FastDDS DataWriter(msg8)"]
        T --> TW
    end
    NW --> SHM --> TR2
    TW --> SHM --> NR
    linkStyle 5 stroke:#ff6666,stroke-width:2px
    linkStyle 6 stroke:#ff6666,stroke-width:2px
    linkStyle 7 stroke:#66cc66,stroke-width:2px
    linkStyle 8 stroke:#66cc66,stroke-width:2px
```

### A-2. 业务流程图（丢包位置）

```mermaid
sequenceDiagram
    participant N as nfoam
    participant SEG as SHM 段
    participant T as task 线程
    N->>SEG: 发 32K JSON(msg7)
    Note over SEG: ★ 丢包点①: SHM 段满(大突发竞态)
    SEG->>T: task 收
    Note over T: 简析 JSON + 响应(msg8)
    T->>SEG: 响应(msg8)
    Note over SEG: 单订阅者 topic 双投（dup，非丢）
    SEG->>N: nfoam 收 + 量测
    Note over N: 零丢到 2000, 往返 avg 430~550us<br/>双投 50~500（速率相关）
```

---

## 3. 基准 B：data 单向接收（spa → data）

### B-1. 部署图（线程级 + 组件 + 丢包点，红=发送）

```mermaid
flowchart TB
    subgraph SP["进程 spa（测试端）"]
        SG["spa 主线程<br/>发 ≤32K 结构体(msg3)"]
        SW["FastDDS DataWriter(msg3)<br/>Writer 历史（不丢）"]
        SG --> SW
    end
    subgraph TRB["FastDDS 传输层（共享内存）"]
        SHMB["SHM 段（512KB ≈ 16 条 32K）<br/>★ 丢包点：段满 + 大突发竞态<br/>实测：data 快消费者零丢到 500/200"]
    end
    subgraph DTSB["进程 dts（被测业务）"]
        DB["FastDDS DataReader(msg3)<br/>data 收 ≤32K 结构体"]
        MBD["Mailbox(data 桶, 无界)<br/>不丢"]
        DT["data 线程<br/>4 字节循环拷贝（业务下限）<br/>不回响应"]
        DB --> MBD --> DT
    end
    SW --> SHMB --> DB
    linkStyle 2 stroke:#ff6666,stroke-width:2px
    linkStyle 3 stroke:#ff6666,stroke-width:2px
```

### B-2. 业务流程图（丢包位置）

```mermaid
sequenceDiagram
    participant S as spa
    participant SEG as SHM 段
    participant D as data 线程
    S->>SEG: 发 ≤32K 结构体(msg3)
    Note over SEG: ★ 丢包点①: SHM 段满(大突发竞态)
    SEG->>D: data 收
    Note over D: 4 字节循环拷贝（模拟业务）<br/>不回响应
    Note over D: 实测零丢到 500<br/>周期+突发夹杂(burst100) 也零丢
```

---

## 4. 丢包归属汇总

| 层/组件 | 是否丢 | 触发条件 |
|---|---|---|
| DataWriter 历史 | ❌ | write_fail=0 |
| **SHM 段 512KB** | ✅ | **紧突发 + 32K + count≥80**（间歇） |
| Mailbox 桶（无界） | ❌ | 排空 + 无界 |
| 业务线程 | ❌ | 按序消费 |
| 单订阅者响应 topic | ⚠️ | 双投（重复上报，非丢） |
| task 业务封顶 kMaxTasks=64 | ⚠️ | 建任务数 >64（业务规则） |

**结论**：真实业务（1s 周期 data + 阶段性 task 配置变更）在稳定区（非紧突发 ≥80 条 32K）→ 不触发。M 级消息是另一个量级，需升级/自研。
