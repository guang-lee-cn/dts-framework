# 2026-08-05：ISO 重构落地（detmw v2 + bootstrap Run 化）

## 事件

用户启动 ISO 重构，调整顺序（detmw 前置，用户指定），完成 detmw v2 全 C++ 化 + bootstrap 完整重构，建远程仓库推送。

## 完成项

### ISO / 设计
- 读取重构备忘录，确认 Strategy 终稿，Operation 写入 docs/iso-output.md
- Step 5 接口契约五份（contracts/: detmw/infrastructure/contexts/bootstrap/detsched）
- 日志模块设计：TsRotatingSink（时间戳文件名+大小切分+总量删除）、每线程分文件、共享单线程池
- 参考项目要求落盘 docs/参考项目要求.md

### detmw v2（用户指定前置，D10）
- 全 C++：`detmw::Communicator` + `detmw::endpoint`（==/hash/ToString 内置）
- `TransportInterface` 抽象隔离底层 DDS（CreateReader/CreateWriter/Send，换 FastDDS 只重写实现）
- 删 detmw.c / detmw_fastdds.h（旧 C 层）
- 双 API：`publish_external`（进程外 DDS）/ `publish_internal`（进程内，task→data 已用）

### bootstrap
- `StartUp/ShutDown` → `Run/Stop`（文件 run.h/cpp）
- Run 阻塞常驻（cv 等待 Stop，不依赖信号）+ 反序下电
- Process{comm, Worker×3} + Worker 自包含（ctx/h/subs），Process::Start()/Stop() 门面
- 修复：pause() 依赖信号唤醒 → 条件变量（测试线程调 Stop 可唤醒）

### 基础设施
- 函数入参 ≤5 写入编码规范（/home/guang/code/CLAUDE.md）

## 验证
- 编译零 warning + ctest 3/3 过（integration/cross_process/s_level_perf）

## 远程
- gh repo create guang-lee-cn/dts-framework（private）
- 推送 refactor + master 分支

## 追加：日志门面 dts::log（下午，用户指定先做日志重构）

- 门面 [log.h](../../infrastructure/include/log.h) + [log.cpp](../../infrastructure/src/log.cpp)（target `dts_log`）：变参模板转发 spdlog，保留 fmt 编译期格式检查；`Log(Level, ...)` + Info/Warn/Error/Debug/Trace + Init（幂等）/Shutdown/SetLevel/GetLevel
- 依赖方向：spdlog ← dts_log ← {detmw, detsched, infrastructure, agent}；全项目零 spdlog 直接调用（仅门面内部）
- 替换：run.cpp / dts_thread / contexts 三 handler / detmw.cpp / detmw_fastdds / thread_factory 共 40+ 处 spdlog；生产 printf 3 处（task_strategy / dts_agent_api 替换）
- 删死代码 detmw_log.cpp/.h（C 变参封装无人调用）
- 测试修复：integration SegFault 根因——SpaMock 的 agent 线程在 Run 返回 Shutdown 后仍打日志（改前 agent 用 printf 潜伏）；加 `SpaMock::Stop()`，integration 先停 agent 再 `Stop()`
- main.cpp 进程提示保留 printf：Run 返回时日志线程池已回收（5b44aca），Shutdown 后走 dts::log 会访问已销毁的 default logger（gdb 实证），CLI 提示不依赖日志系统
- 验证：编译零 warning + ctest 3/3 过 + cpf_dts SIGINT 优雅退出（exit 0 + done）

## 追加：TsRotatingSink 文件落地（下午，log 收尾）

- [ts_rotating_sink.cpp](../../infrastructure/src/ts_rotating_sink.cpp)（继承 base_sink）：时间戳文件名（同秒切分追加序号消歧）+ 单文件 5MB 切分 + 目录总量删旧（mtime 最旧，总量含当前文件但不删当前）
- Config 增 file 段（enable/dir/namePattern/maxSizeMb/maxTotalMb），Init 支持 console+file 双 sink；目录不可写 → stderr 提示一次后静默降级（console 不受影响）
- 验证：6000 行 × 512B 慢速写 → 切分 4 文件行号连续、总量超限删最旧、剩余 ≤ maxTotalMb（含当前）；ctest 3/3 过；cpf_dts SIGINT 优雅退出
- 契约 §5 / CONTEXT 已更新；待办移除 TsRotatingSink，改挂"每线程 logger"

## 追加：bootstrap run 检视处理（bootstrap-run-review-2026-08-05）

10 条逐条裁决：

### 修改（7 条）
- **R1** ✅ 装配失败契约兑现：`Communicator::good()` 暴露失败态；Run 检查 detmw init 失败（返回 2）/ 线程装配失败（返回 3）；实测坏 cfg → "detmw init failed" + main 非 0 退出
- **R8** ✅ 并入 R1：`DeclareDomain` 返回值 / `CreateThread` 返回 nullptr 计入装配失败，`Start()` 返回 bool，`StopWorker` 对未建线程跳过
- **R4** ✅ 删 dts_def.h `SessionKey`/`SessionKeyHash`（全仓库零使用，寻址键统一 detmw::endpoint）
- **R6** ✅ 删 Worker.name 死字段（线程名由 ctx.Init 与 detsched 各自持有）
- **R7** ✅ detmw.h publish_internal 注释对齐现状（当前走 transport，mailbox 直通为后续优化）
- **R10** ✅ run.h 注明"进程生命周期内仅执行一次"
- **R2** ⚠️ 不改 shared_ptr：`Process::Stop()` 注释固化假设——FastDDS `delete_participant` 阻塞等待接收线程退出，`comm.reset()` 后 `subs.clear()` 无 UAF；换传输实现需重新验证

### 驳回（2 条，理由记录）
- **R5** 三 Worker 表驱动：业务线程固定三（task/data/log 是业务域定义非可扩展集合），命名成员可读性优于数组索引；生成路由类型异构（TaskRoute/DataRoute/LogRoute），表驱动需统一类型得不偿失。未来业务线程动态化时重议
- **R9** RequestStop/WaitStop 纯转发：保留方法——给 Process 公开接口业务语义（请求/等待停止），`stop` 为内部实现细节，直接暴露使调用方依赖内部类型

### 部分采纳（1 条）
- **R3** detmw 双所有权：不重构（下电顺序 `DtsMwSet(nullptr)` → `comm.reset()` 固定正确无悬挂），dts_mw.h 注释固化非拥有约定；弱引用/单例彻底收口入迭代计划

### 争议 → 迭代计划
- subscribe 部分失败：当前"降级继续"（log + 继续），是否"失败即停"待决策
- R2 `delete_participant` join 假设正式验证（高吞吐压力 + ASan）
- R3 detmw 所有权彻底收口（弱引用语义 / Communicator 全局单例）

### 验证
编译零 warning + ctest 3/3 + 坏 cfg 装配失败返回非 0 + SIGINT 优雅退出

## 追加：console 控制面落地（下午，按 CONTEXT-console.md 启动）

- [ctl.h](../../infrastructure/include/ctl.h) + [ctl.cpp](../../infrastructure/src/ctl.cpp)：`dts::ctl::Command` / `CommandRegistry`（单例 Register/Find/ForEach）/ `Execute(line, out)`；内置 help / get_threads / set_log_level（RegisterBuiltins 幂等）
- [console.h](../../infrastructure/include/console.h) + [console.cpp](../../infrastructure/src/console.cpp)：`console_start/stop`（AF_UNIX 行协议 socket 监听线程，只投递不执行）+ `control_start/stop`（执行线程，消费队列 → ctl::Execute → 回写）；BKG 域低优先级（SCHED_OTHER 不绑 RT）
- 命令队列：Job{fd, line} + mutex/cv；console 短连接读行投递，control 执行回写后 close
- bootstrap 装配：Process::Start 末尾 console_start(派生 sock 路径)/control_start；Stop 反序先停 console/control；sock 路径 cfg 文件名派生（/tmp/dts-cpf-dts.sock，多实例不冲突）
- CMake：infrastructure 增 ctl.cpp/console.cpp + link detsched（get_threads 用 QueryThreads）
- 验证：编译零 warning + ctest 3/3 + socket 手动（help 三条命令 / get_threads 5 线程 / set_log_level debug 生效 / SIGINT 优雅退出 exit 0）
- 已知：get_threads 业务线程 prio 显示 0（detsched fallback 路径句柄 m_prio=0，非 console 问题，见 CONTEXT 待办）

## 追加：console 交互会话 + dts CLI（下午，用户期望 REPL 登录）

- console.cpp 短连接改**长连接会话**：accept 每连接起会话线程，读行投递 control，同步等待响应回写（`\x00` 分隔）；control_stop 清队列唤醒 pending 会话防 join 死锁
- [dts-cli.py](../../tools/dts-cli.py)：`login <name>`（REPL，`# ` 提示符，quit 退出）/ `exec <name> <cmd>`（单条）/ `ps`（扫描 /tmp/dts-*.sock）
- ctl.cpp 未知命令消息改 `not found: xxx`（对齐设备 CLI 心智）
- 验证：编译零 warning + ctest 3/3 + CLI 实测（ps 发现实例 / exec help / exec 未知命令 not found / login 多命令会话 / SIGINT 优雅退出）

## 待办（更新后，含检视迭代计划）
- [ ] infrastructure：console 模块（CommandExecutor 命令表 + socket 监听线程 + control 线程）——主线
- [ ] **决策**：subscribe 部分失败策略（当前降级继续，检视 R1 遗留：失败即停 vs 部分降级）
- [ ] 日志：每线程 logger（dts_data/dts_task/dts_log，当前单 default logger，文件名 {} 恒 "dts"）
- [ ] **验证 R2**：FastDDS `delete_participant` join 假设（高吞吐压力 + ASan 下确认无 UAF）
- [ ] detmw 收尾：publish_internal mailbox 直通（transport 内部直投 + 仍走 OnRouteMsg 统一路由，契约 detmw.md §3）
- [ ] detsched / contexts 逐层重写
