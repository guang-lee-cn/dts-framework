# dts-framework —— Agent 工作约定

> 本文件按 `AGENTS.md` 约定被 DSH / Codex / Antigravity 等自动加载；Claude Code 的 `.claude/` 配置与本文件并存，互不覆盖。

## 项目速览

电信网管 / 运维数据采集场景的 **C++17 多进程通信中间件**。

- 分层：`bootstrap`（组合根）→ `contexts`（限界上下文）→ `infrastructure` → `detsched`（确定性调度）/ `detmw`（通信中间件）
- 跨进程通信收敛到 `Communicator`，传输层被 `TransportInterface` 隔离；**业务代码不直接调用 DDS API**
- 构建 CMake / 测试 ctest
- **已有教训**：自研共享内存桶传输曾因"抄了内置 SHM 的返回值语义、却按 UDP 方式分配接收缓冲"导致 OOM。改基础层之前，先确认你读懂了被参考实现的**语义**，而不只是它的返回值

## 工作模式：懒惰的资深工程师

> 规则集来源：[DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail)（MIT，Copyright (c) 2026 DietrichGebert）。以下按本项目做了最小改写。

写代码前，停在第一个成立的台阶：

1. 这东西需要存在吗？（YAGNI）
2. 代码库里已经有了吗？复用已有的 helper / util / 模式，别重写
3. 标准库能做吗？用它
4. 平台原生能力覆盖了吗？用它
5. 已装的依赖能解决吗？用它
6. 能一行吗？写成一行
7. 只有到这一步：写能跑的最小实现

**阶梯在理解问题之后跑，不是代替理解**：先读完任务与它触及的代码，把真实调用链走通，再爬台阶。

**改 bug 找根因，不找症状**：报告描述的是症状。grep 你改的函数的**所有调用方**，把共享函数改一次——在那里加一个护栏，diff 比每个调用方各加一个更小；只修 ticket 点到的那条路径，会留下兄弟调用方仍然坏着。

**规则**：不建没被明确要求的抽象；能不加新依赖就不加；不写没人要的样板；删除优先于新增；无聊优先于聪明；文件越少越好；最短可用 diff 获胜，但必须在理解问题之后。

**标出刻意的简化**：为省事而砍掉真实角落的地方（全局锁、O(n²) 扫描、朴素启发式），用 `ponytail:` 注释写明天花板与升级路径——让"以后再说"不会变成"永远不说"。

**不偷懒的地方**：理解问题本身；信任边界的输入校验；防止数据丢失的错误处理；安全；**真实硬件需要的标定**（平台从来不是规格里的理想状态，时钟会漂移，传感器读数会偏）；以及任何被明确要求的东西。非平凡逻辑留**一处**可运行的检查（断言式自检或一个小测试文件，不要框架、不要 fixture）；一行流不需要测试。

## 与 iso 技能的分工

`iso`（本项目的 `.agents/skills/iso` 与 `.claude/skills/iso`）是**阶段门禁与架构决策**层：Insight → Strategy → Operation，强制分层架构、架构图、ADR、接口契约。ponytail 阶梯管**实现阶段的每一行**。

两者在"要不要建抽象"上有张力，冲突时按此表裁决：

| 冲突点 | 谁说了算 |
|---|---|
| 要不要建这个抽象 / 拆这个类 | **ponytail**（iso 的 `references/thinking.md` 本来就要求"质变才升复杂度"） |
| 该不该做设计、画图、写契约与 ADR | **iso**（不能因为"想省事"跳过该做的设计） |
| `operations/cpp.md` 的类尺寸红线触发时 | 优先抽新类；但若判定该抽象**没必要存在**，走 iso 的回滚机制上报，**不要两边各让一半** |
| 基础层 / 公共设施 / 关键路径的选型（自研 vs 开源） | 走 iso 的 `references/oss-adoption.md`，结论写进 strategy 决策记录 |
| 真冲突且两边都有理 | **停下来问人**，不自行折中 |

## 触发词

- "开始 ISO" / "开始 ISO，轻量模式" / "重构 + 开始 ISO"
- "做架构" → "实现子系统 X" → "集成测试"
- ponytail 阶梯默认常开；说"这次别省"或"按 ponytail ultra 来"可显式调整强度
